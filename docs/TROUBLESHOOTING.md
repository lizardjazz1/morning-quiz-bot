# Анализ проблем и решения

## Дата анализа: 2026-02-03

### Проблема 1: SSL ошибки при подключении к Telegram API

**Симптомы:**
```
httpcore.ConnectError: [SSL: RECORD_LAYER_FAILURE] record layer failure (_ssl.c:1029)
telegram.error.NetworkError: httpx.ConnectError: [SSL: RECORD_LAYER_FAILURE] record layer failure
```

**Причина:**
- Проблемы с SSL/TLS handshake при подключении к Telegram API
- Может быть вызвано нестабильным сетевым подключением, проблемами с прокси или блокировкой Telegram в России

**Решения:**

1. **Использовать Telegram Bot API локальный сервер** (рекомендуется):
```bash
# Установка локального Telegram Bot API сервера
git clone https://github.com/tdlib/telegram-bot-api.git
cd telegram-bot-api
mkdir build && cd build
cmake -DCMAKE_BUILD_TYPE=Release ..
cmake --build . --target install

# Запуск локального сервера
telegram-bot-api --api-id=YOUR_API_ID --api-hash=YOUR_API_HASH
```

Затем в `bot.py` изменить инициализацию Application:
```python
application = (
    Application.builder()
    .token(bot_token)
    .base_url("http://localhost:8081/bot")  # Локальный сервер
    .read_timeout(60)
    .write_timeout(60)
    .connect_timeout(30)
    .pool_timeout(60)
    .build()
)
```

2. **Увеличить таймауты и retry логику** (временное решение):

В `bot.py` увеличить таймауты:
```python
application = (
    Application.builder()
    .token(bot_token)
    .read_timeout(120)  # Увеличено с 60
    .write_timeout(120)
    .connect_timeout(60)  # Увеличено с 30
    .pool_timeout(120)
    .get_updates_read_timeout(180)  # Специально для long polling
    .build()
)
```

3. **Использовать VPN или прокси**:

Добавить в `.env`:
```
HTTPS_PROXY=http://your-proxy-server:port
HTTP_PROXY=http://your-proxy-server:port
```

И в `bot.py`:
```python
import httpx

# Настройка прокси для httpx
proxy_url = os.getenv('HTTPS_PROXY')
if proxy_url:
    proxy_client = httpx.AsyncClient(proxy=proxy_url)
    application = (
        Application.builder()
        .token(bot_token)
        .get_updates_http_version("1.1")
        .http_version("1.1")
        # ... остальные настройки
        .build()
    )
```

4. **Downgrade Python версии** (если проблема в Python 3.13):

Python 3.13 может иметь проблемы с некоторыми SSL библиотеками. Попробуйте использовать Python 3.11 или 3.12:
```bash
# Установка Python 3.12
pyenv install 3.12.0
pyenv local 3.12.0

# Пересоздание виртуального окружения
rm -rf venv
python3.12 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

### Проблема 2: Система весов категорий сбрасывается после перезагрузки

**Симптомы:**
- После перезапуска сервера статистика использования категорий теряется
- Weighted selection не работает или работает неправильно

**Анализ кода:**

1. **Сохранение работает правильно** (`modules/category_manager.py:296`):
```python
# Сохраняем глобальную статистику при каждом обновлении
self._save_category_usage_stats()
```

2. **Загрузка происходит только при инициализации** (`modules/category_manager.py:108`):
```python
def __init__(self, ...):
    self._load_category_usage_stats()  # Загружается один раз
```

3. **Обновление статистики** (`handlers/quiz_manager.py:907`):
```python
self.data_manager.category_manager._update_category_usage_sync(category, chat_id)
```

**Возможные причины:**

1. **Файл `data/statistics/categories_stats.json` не сохраняется**
   - Проверьте права доступа к директории `data/statistics/`
   - Проверьте, что файл создается и содержит данные

2. **CategoryManager пересоздается при каждом запросе**
   - Проверьте, что CategoryManager создается только один раз в `bot.py:268`

3. **Статистика не загружается при старте**
   - Проверьте логи на наличие ошибок загрузки

**Решение:**

1. **Проверить файл статистики**:
```bash
# Проверить существование файла
ls -la data/statistics/categories_stats.json

# Посмотреть содержимое
cat data/statistics/categories_stats.json | jq '.'

# Проверить права доступа
chmod 644 data/statistics/categories_stats.json
chmod 755 data/statistics/
```

2. **Добавить диагностическую команду** в `handlers/common_handlers.py`:
```python
async def debug_category_stats(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Показывает текущую статистику категорий"""
    if not await is_user_admin_in_update(update):
        await update.message.reply_text("❌ Эта команда доступна только администраторам")
        return

    stats = self.category_manager._category_usage_stats

    message = "📊 Статистика категорий:\n\n"
    for cat_name, cat_stats in sorted(stats.items(), key=lambda x: x[1].get('global_usage', 0), reverse=True)[:10]:
        global_usage = cat_stats.get('global_usage', 0)
        total_q = cat_stats.get('total_questions', 0)
        message += f"• {cat_name}: {global_usage} использований, {total_q} вопросов\n"

    await update.message.reply_text(message)
```

3. **Добавить проверку целостности при загрузке** в `modules/category_manager.py`:
```python
def _load_category_usage_stats(self) -> None:
    """Загружает статистику использования категорий из файла"""
    try:
        stats_file = self._get_stats_file_path()
        logger.info(f"🔍 Загрузка статистики категорий из {stats_file}")

        if stats_file.exists():
            with open(stats_file, 'r', encoding='utf-8') as f:
                loaded_data = json.load(f)

            if loaded_data and isinstance(loaded_data, dict):
                self._category_usage_stats = loaded_data
                logger.info(f"✅ Загружено {len(self._category_usage_stats)} категорий")

                # Выводим топ-5 для проверки
                top_cats = sorted(self._category_usage_stats.items(),
                                 key=lambda x: x[1].get('global_usage', 0),
                                 reverse=True)[:5]
                for cat, stats in top_cats:
                    logger.info(f"  • {cat}: {stats.get('global_usage', 0)} использований")
            else:
                logger.warning("Файл статистики пуст или неверного формата")
                self._category_usage_stats = {}
        else:
            logger.warning(f"❌ Файл статистики не найден: {stats_file}")
            self._category_usage_stats = {}

    except Exception as e:
        logger.error(f"❌ Ошибка загрузки статистики: {e}", exc_info=True)
        self._category_usage_stats = {}
```

4. **Добавить периодическое сохранение статистики** в `bot.py`:
```python
# После инициализации application
async def periodic_stats_save(context: ContextTypes.DEFAULT_TYPE):
    """Периодически сохраняет статистику категорий"""
    try:
        category_manager._save_category_usage_stats()
        logger.debug("💾 Периодическое сохранение статистики категорий")
    except Exception as e:
        logger.error(f"Ошибка периодического сохранения статистики: {e}")

# Добавляем задачу в job queue (каждые 5 минут)
application.job_queue.run_repeating(
    periodic_stats_save,
    interval=300,  # 5 минут
    first=60  # Первый запуск через 1 минуту
)
```

---

### Проблема 3: Начисление баллов после неверных ответов (возможно ложная тревога)

**Анализ:**
После изучения кода в `modules/score_manager.py:211-258` выяснилось:

1. **Логика начисления очков правильная**:
   - Правильный ответ: `+1` очко (или больше с бонусом за streak)
   - Неправильный ответ: `-0.5` очка

2. **Проверка ачивок происходит ДО обновления очков** (строка 166):
```python
current_score_for_motivation = current_user_data_global.get("score", 0)
# Проверка ачивок с ТЕКУЩИМ счетом
# ...
# Потом обновление очков (строка 248)
current_user_data_global["score"] = current_score - 0.5
```

3. **Streak ачивки НЕ начисляются при неправильных ответах** (строка 273):
```python
if is_correct:  # Только при правильном ответе
    # ... проверка streak ачивок
```

**Вывод:**
Проблема скорее всего связана с задержками из-за SSL ошибок, а не с логикой начисления баллов. Пользователь может видеть сообщения о достижениях с задержкой, что создает впечатление неправильной работы.

**Рекомендации:**
1. Решить проблему с SSL ошибками (см. Проблему 1)
2. После исправления сетевых проблем проверить, остается ли проблема
3. Если проблема сохраняется, добавить более подробное логирование в `score_manager.py`

---

## Приоритет исправлений

1. **Высокий**: SSL ошибки - они влияют на всю работу бота
2. **Средний**: Система весов категорий - влияет на разнообразие квизов
3. **Низкий**: Начисление баллов - скорее всего следствие проблемы #1

## Следующие шаги

1. Реализовать локальный Telegram Bot API сервер (самое надежное решение)
2. Добавить диагностическую команду для проверки статистики категорий
3. Добавить периодическое сохранение статистики
4. Мониторить логи после применения исправлений
