# Скрипты для Morning Quiz Bot

Эта папка содержит вспомогательные скрипты для управления ботом и его данными.

## 🖼️ Скрипт конвертации изображений

### `convert_and_metadata.py` ⭐ **ОСНОВНОЙ!**
Универсальный скрипт для конвертации изображений в WebP **с автоматическим добавлением метаданных** в `photo_quiz_metadata.json`.

**Возможности:**
- Конвертация PNG, JPG, JPEG, BMP, TIFF, GIF в WebP
- Автоматическая генерация подсказок на основе имени файла
- Добавление метаданных в JSON файл
- Режим только добавления метаданных для существующих WebP

**Использование:**
```bash
# Полная конвертация + метаданные
python scripts/convert_and_metadata.py

# Только добавление метаданных для существующих WebP
python scripts/convert_and_metadata.py --metadata-only
```

## 📋 Параметры конвертации

- **Качество WebP**: 85% (оптимальный баланс размер/качество)
- **Оптимизация**: Включена
- **Прозрачность**: Конвертируется в белый фон
- **Оригиналы**: Удаляются после успешной конвертации

## ⚠️ Важные замечания

1. **Резервные копии**: Рекомендуется создать резервную копию папки `data/images` перед конвертацией
2. **Необратимость**: После удаления оригиналов восстановление возможно только из резервных копий
3. **Зависимости**: Требуется библиотека Pillow (`pip install Pillow`)

## 📊 Результаты конвертации

После конвертации всех изображений:
- **PNG → WebP**: ~195 MB экономии
- **JPG → WebP**: ~4.6 MB экономии
- **Общая экономия**: ~200 MB
- **Файлов**: 159 WebP изображений готовы для фото-викторины
- **Метаданные**: Автоматически генерируются для всех новых изображений

## 🎯 Рекомендуемый рабочий процесс

1. **Добавление новых изображений:**
   ```bash
   # Поместите новые PNG/JPG файлы в data/images/
   # Запустите конвертацию с метаданными
   python scripts/convert_and_metadata.py
   ```

2. **Обновление метаданных для существующих WebP:**
   ```bash
   # Если нужно добавить метаданные для уже конвертированных файлов
   python scripts/convert_and_metadata.py --metadata-only
   ```
