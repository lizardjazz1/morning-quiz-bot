# Система уведомлений разработчику

## Обзор

Система уведомлений разработчику автоматически отправляет сообщения в Telegram о проблемах в системе, требующих внимания разработчика.

## Возможности

### 🚨 Уведомления о проблемных вопросах
- Автоматическое обнаружение малформированных JSON файлов
- Детальная информация об ошибках (тип, категория, описание)
- Уведомления о новых проблемах при загрузке данных

### ⚠️ Уведомления об ошибках данных
- Ошибки при сохранении/загрузке данных
- Проблемы с синхронизацией
- Ошибки обновления статистики

### ✅ Уведомления об автоисправлении
- Информация об успешно исправленных файлах
- Статистика автоматических исправлений

### 💥 Системные ошибки (опционально)
- Критические ошибки системы
- Проблемы с модулями

## Настройка

### 1. Установка Developer User ID

Чтобы получить ваш Telegram User ID:
1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Бот покажет ваш ID (например: `123456789`)

### 2. Настройка через скрипт

```bash
python setup_developer_notifications.py
```

Скрипт проведет вас через процесс настройки:
- Ввод Developer User ID
- Включение/отключение уведомлений
- Настройка типов уведомлений

### 3. Ручная настройка

Отредактируйте `config/quiz_config.json`:

```json
{
  "global_settings": {
    "developer_notifications": {
      "enabled": true,
      "developer_user_id": 123456789,
      "notify_on_malformed_questions": true,
      "notify_on_data_errors": true,
      "notify_on_system_errors": false
    }
  }
}
```

## Конфигурация

### Параметры

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `enabled` | boolean | Включить/отключить уведомления | `false` |
| `developer_user_id` | integer | Telegram User ID разработчика | `null` |
| `notify_on_malformed_questions` | boolean | Уведомления о проблемных вопросах | `true` |
| `notify_on_data_errors` | boolean | Уведомления об ошибках данных | `true` |
| `notify_on_system_errors` | boolean | Уведомления о системных ошибках | `false` |

### Рекомендуемые настройки

```json
{
  "developer_notifications": {
    "enabled": true,
    "developer_user_id": YOUR_USER_ID,
    "notify_on_malformed_questions": true,
    "notify_on_data_errors": true,
    "notify_on_system_errors": false
  }
}
```

## Использование

### Автоматические уведомления

Система автоматически отправляет уведомления при:
- Обнаружении проблемных файлов вопросов
- Ошибках при работе с данными
- Успешном автоисправлении файлов

### Тестирование

После настройки протестируйте систему командой:
```
/test_notifications
```

### Интеграция в код

```python
from modules.developer_notifier import DeveloperNotifier

# Создание уведомлений
notifier = DeveloperNotifier(bot, app_config)

# Установка в DataManager
data_manager.set_developer_notifier(notifier)

# Ручная отправка уведомления
notifier.notify_data_error("test_error", "Тестовая ошибка", "Контекст")
```

## Примеры уведомлений

### 🚨 Проблемы с вопросами
```
🚨 Проблемы с вопросами (2025-01-25 15:30:45)

Найдено проблемных файлов: 2

1. Спорт (load_error)
   Ошибка: Extra data: line 703 column 1 (char 14081)

2. Математика (invalid_question)
   Ошибка: Missing required field 'options'
```

### ✅ Автоисправление
```
✅ Автоматическое исправление (2025-01-25 15:31:20)

Исправлено файлов: 1

1. Спорт
```

### ⚠️ Ошибка данных
```
⚠️ Ошибка данных (2025-01-25 15:32:10)

Тип: save_malformed_error
Детали: Permission denied
Контекст: Сохранение малформированных вопросов
```

## Безопасность

- Уведомления отправляются только в личку указанному пользователю
- Система проверяет корректность User ID
- Ошибки отправки уведомлений не влияют на основную работу бота
- Логирование всех попыток отправки уведомлений

## Устранение неполадок

### Уведомления не приходят

1. Проверьте, что `enabled: true`
2. Убедитесь, что `developer_user_id` установлен корректно
3. Проверьте, что бот может писать в личку пользователю
4. Убедитесь, что пользователь не заблокировал бота

### Ошибки при отправке

1. Проверьте логи бота на наличие ошибок
2. Убедитесь, что у бота есть права на отправку сообщений
3. Проверьте корректность User ID

### Тестирование

```bash
# Проверка настроек
python setup_developer_notifications.py

# Выберите опцию 2 для проверки текущих настроек
```

## Разработка

### Добавление новых типов уведомлений

1. Добавьте новый параметр в конфигурацию
2. Создайте соответствующий метод в `DeveloperNotifier`
3. Интегрируйте вызов в нужные места кода

### Расширение функциональности

- Групповые уведомления для команды разработчиков
- Настройка частоты уведомлений
- Фильтрация по важности проблем
- Интеграция с внешними системами мониторинга

## Заключение

Система уведомлений разработчику обеспечивает:
- Быстрое реагирование на проблемы
- Автоматический мониторинг качества данных
- Прозрачность работы системы
- Упрощение отладки и поддержки

Настройте систему уведомлений для эффективного мониторинга вашего бота!
